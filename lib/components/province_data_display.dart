import 'package:flutter/material.dart';

class ProvinceDataDisplay extends StatelessWidget {
  const ProvinceDataDisplay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
