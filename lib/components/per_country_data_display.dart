import 'package:flutter/material.dart';

class PerCountryDataDisplay extends StatelessWidget {
  const PerCountryDataDisplay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
